import styled from 'react-emotion';

const AExt = styled.a`
  text-decoration: underline dotted;
  text-decoration-color: white;
  .link-icon {
    color: white;
    svg {
      height: 10px;
    }
  }
`;

export default AExt;
